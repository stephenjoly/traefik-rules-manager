http:
  routers:
    ollama-rtr:
      rule: 'Host(`ollama.{{ env "DOMAINNAME_1" }}`)'
      entryPoints:
        - websecure
      tls:
        certResolver: dns-cloudflare
        options: tls-opts@file

  services:
    ollama-svc:
      loadBalancer:
        # passHostHeader: true
        # serversTransport: "ollama"
        servers:
          - url: 'http://10.87.111.50:11434'

  # serversTransports:
  #   ollama:
  #     insecureSkipVerify: true