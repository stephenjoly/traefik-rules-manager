http:
  routers:
    pihole-rtr:
      rule: 'Host(`pihole.{{ env "DOMAINNAME_1" }}`)'
      entryPoints:
        - websecure
      service: pihole-svc
      tls:
        certResolver: dns-cloudflare

  services:
    pihole-svc:
      loadBalancer:
        servers:
          - url: 'https://10.87.111.239/'